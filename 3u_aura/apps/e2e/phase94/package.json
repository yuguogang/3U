{
  "name": "3u-aura-phase94-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "fork:create": "node ../../../scripts/uat/create-weekly-fork-env.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "fork:db:prepare": "node ../../../scripts/uat/prepare-weekly-fork-db.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "fork:db:reset": "node ../../../scripts/uat/reset-weekly-fork-db.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "fork:start": "node ../../../scripts/uat/start-weekly-fork.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "fork:stop": "node ../../../scripts/uat/stop-weekly-fork.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "fork:reset": "node ../../../scripts/uat/reset-weekly-fork.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "fork:advance-time": "node ../../../scripts/uat/advance-weekly-fork-time.mjs --env ${PROMOTION_ENV:-fork-anvil}",
    "stack:start": "node ../../../scripts/uat/start-promotion-services.mjs --env ${PROMOTION_ENV:-uat-mockusdt}",
    "stack:stop": "node ../../../scripts/uat/stop-promotion-services.mjs --env ${PROMOTION_ENV:-uat-mockusdt}",
    "test": "playwright test",
    "test:core": "pnpm run stack:start && pnpm run wallets:prepare && pnpm run test:precheck && playwright test --grep @phase94-core",
    "manual:core": "pnpm run test:core",
    "test:precheck": "playwright test tests/precheck.spec.ts",
    "test:smoke": "playwright test --grep @smoke",
    "test:uat": "pnpm run stack:start && pnpm run wallets:prepare && pnpm run test:precheck && playwright test tests/smoke tests/core",
    "manual:uat": "pnpm run test:uat",
    "test:weekly-pack": "PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run fork:start && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run fork:db:reset && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run stack:stop || true && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run stack:start && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run wallets:prepare && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/fork-precheck.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/subsidy-claim.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/rollover.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/threshold-met.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/lottery-blocked.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/lottery.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/ranking-blocked.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/ranking.spec.ts && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork/merkle-claim.spec.ts",
    "test:weekly-fork": "PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run fork:start && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run fork:db:reset && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run stack:stop || true && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run stack:start && PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} pnpm run wallets:prepare && HEADLESS=true PROMOTION_ENV=${PROMOTION_ENV:-fork-anvil} playwright test tests/weekly-fork",
    "manual:weekly-fork": "pnpm run test:weekly-fork",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "wallets:prepare": "node ../../../scripts/promotion-env/prepare-wallet-fixtures.mjs --env ${PROMOTION_ENV:-uat-mockusdt}",
    "wallets:build": "synpress",
    "wallets:build:serial": "node ./scripts/build-wallet-cache.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.48.2",
    "@synthetixio/synpress": "4.0.5",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3",
    "viem": "2.45.1"
  }
}
